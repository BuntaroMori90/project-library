import { LibroniaPublicPreview } from "@/components/libronia-public-preview";

export const metadata = {
  title: "Libronia — Anteprima redesign",
  description: "Anteprima pubblica del redesign grafico di Libronia con dati dimostrativi.",
};

export default function LibroniaPreviewPage() {
  return <LibroniaPublicPreview />;
}

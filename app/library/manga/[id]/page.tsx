import Link from "next/link";
import { notFound } from "next/navigation";
import { BookOpen, Check, Circle, Heart, Layers3, LibraryBig, Star, StickyNote } from "lucide-react";
import { requireProfile } from "@/lib/profile";
import { getMangaDetail } from "@/lib/repositories/library";
import { demoManga } from "@/lib/demo-data";
import { DemoCover } from "@/components/demo-cover";\nimport { WishlistToggle } from "@/components/wishlist-toggle";
import { toggleOwnedVolume, updateMangaPersonal, updateMangaProgress, updateMangaState } from "./actions";

const UUID=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
const statusLabels:Record<string,string>={PLANNED:"Da iniziare",IN_PROGRESS:"In lettura",COMPLETED:"Completato",PAUSED:"In pausa",DROPPED:"Abbandonato"};
function DetailTabs(){return <nav className="detail-tabs" aria-label="Sezioni opera"><a href="#panoramica">Panoramica</a><a className="active" href="#volumi">Volumi</a><a href="#capitoli">Capitoli</a><a href="#edizioni">Edizioni</a><a href="#personale">La mia scheda</a></nav>}
function VolumeLegend(){return <div className="volume-legend" aria-label="Legenda volumi"><span><i className="legend-dot read"/> Letto</span><span><i className="legend-dot owned"/> Posseduto</span><span><i className="legend-dot current"/> Attuale</span><span><i className="legend-dot empty"/> Da leggere</span></div>}
function DemoDetail({id}:{id:string}){const item=demoManga.find(manga=>manga.id===id);if(!item)notFound();const totalVolumes=id==="berserk"?42:id==="monster"?9:12;const readThrough=id==="berserk"?25:totalVolumes;const ownedThrough=id==="berserk"?18:id==="monster"?totalVolumes:4;const chapter=id==="berserk"?201:null;return <main className="page manga-detail-page work-detail-v2"><Link href="/library/manga" className="back-link">← Manga</Link><section className="work-hero"><div className="work-cover-wrap"><DemoCover item={item}/><button className="round-favorite" aria-label="Preferito"><Heart size={18}/></button></div><div className="work-identity"><p className="eyebrow">Manga · catalogo demo</p><h1>{item.title}</h1><p className="work-original">ベルセルク</p><p className="work-byline">{item.creator} · 1989 · In pubblicazione</p><div className="meta-pills"><span>Seinen</span><span>Dark fantasy</span><span>Avventura</span></div><p className="work-description">Un'opera catalogata una sola volta; lettura, possesso, voto e note restano invece personali e indipendenti.</p></div></section><section className="personal-dashboard" id="personale"><div className="personal-dashboard-head"><div><span className="eyebrow">La mia scheda</span><h2>{item.title} nella mia libreria</h2></div><button className="soft-action">Modifica</button></div><div className="personal-metrics"><div><span>Stato</span><strong>In lettura</strong><small>Gestito qui, non dalla Home</small></div><div><span>Progresso</span><strong>Vol. {readThrough} · Cap. {chapter}</strong><small>Ultimo punto registrato</small></div><div><span>Posseduti</span><strong>{ownedThrough} / {totalVolumes}</strong><small>Berserk Collection</small></div><div><span>Il mio voto</span><strong>9,5 <Star size={15} fill="currentColor"/></strong><small>Personale</small></div></div></section><DetailTabs/><section id="panoramica" className="detail-section overview-grid"><article className="overview-card"><span className="eyebrow">Opera</span><h2>Panoramica</h2><dl><div><dt>Autore</dt><dd>{item.creator}</dd></div><div><dt>Stato editoriale</dt><dd>In pubblicazione</dd></div><div><dt>Volumi catalogati</dt><dd>{totalVolumes}</dd></div><div><dt>Capitoli</dt><dd>Numerazione separata</dd></div></dl></article><article className="overview-card note-preview"><StickyNote size={20}/><span className="eyebrow">Le mie note</span><p>Una nota personale breve può essere mostrata qui; la modifica completa resta nella sezione “La mia scheda”.</p></article></section><section id="volumi" className="detail-section volumes-stage"><div className="section-heading detail-heading"><div><span className="eyebrow">Catalogo + collezione</span><h2>Tutti i volumi</h2></div><span>{totalVolumes} nel catalogo demo</span></div><p className="subtitle detail-explainer">Vedi sempre l'intera serie. Lettura e possesso sono due livelli separati.</p><VolumeLegend/><div className="volume-grid volume-grid-visual">{Array.from({length:totalVolumes},(_,index)=>index+1).map(volume=>{const read=volume<=readThrough;const owned=volume<=ownedThrough;const current=volume===readThrough;return <article key={volume} className={`volume visual-volume ${owned?"owned":""} ${read?"read":""} ${current?"current":""}`}><div className="volume-spine"><span>{String(volume).padStart(2,"0")}</span></div><div className="volume-copy"><strong>Volume {volume}</strong><span>{current?"Volume attuale":read?"Letto":"Da leggere"}</span><small>{owned?"Posseduto":"Non posseduto"}</small></div></article>})}</div></section><section id="capitoli" className="detail-section two-up-detail"><article className="info-panel"><BookOpen size={22}/><div><span className="eyebrow">Capitoli</span><h2>Progresso preciso</h2><p>Segna semplicemente “letto fino al capitolo 201”. L'elenco completo dei capitoli viene mostrato solo quando la fonte dati fornisce numerazioni reali e speciali.</p></div></article><article className="info-panel"><Layers3 size={22}/><div><span className="eyebrow">Edizioni</span><h2>Collezione fisica</h2><p>Il progresso appartiene all'opera; i volumi posseduti appartengono invece alla specifica edizione scelta.</p></div></article></section><section id="edizioni" className="detail-section"><div className="section-heading detail-heading"><div><span className="eyebrow">Edizioni</span><h2>Le versioni di {item.title}</h2></div></div><div className="edition-row"><article className="edition-choice selected"><strong>Berserk Collection</strong><span>Edizione posseduta · 18 / {totalVolumes}</span></article><article className="edition-choice"><strong>Deluxe / altre edizioni</strong><span>Arricchimento catalogo</span></article></div></section></main>}

export default async function MangaDetailPage({params}:{params:Promise<{id:string}>}){const{id}=await params;if(!UUID.test(id))return <DemoDetail id={id}/>;const{profile}=await requireProfile();const detail=await getMangaDetail(profile.id,id);const{work,creators,libraryEntry,progress,editions,canonicalEdition,volumes,ownedIds}=detail;if(!work)notFound();const currentVolume=progress?.current_volume!=null?Number(progress.current_volume):null;const currentChapter=progress?.current_chapter!=null?Number(progress.current_chapter):null;const totalVolumes=work.total_volumes??canonicalEdition?.total_units??null;const ownedCount=ownedIds.size;const state=libraryEntry?.status??"PLANNED";const statusText=statusLabels[state]??"Da iniziare";const publicationLabel=work.publication_status==="ONGOING"?"In pubblicazione":work.publication_status==="COMPLETED"?"Concluso":work.publication_status==="HIATUS"?"In pausa editoriale":"Stato da verificare";return <main className="page manga-detail-page work-detail-v2"><Link href="/library/manga" className="back-link">← Manga</Link><section className="work-hero real-work-hero"><div className="work-cover-wrap"><div className="real-cover-shell">{work.cover_url?<img className="real-cover" src={work.cover_url} alt={`Copertina di ${work.title}`}/>:<div className="cover-placeholder">{work.title.slice(0,1)}</div>}</div>{libraryEntry?.favorite?<span className="favorite-float"><Heart size={16} fill="currentColor"/> Preferito</span>:null}</div><div className="work-identity"><p className="eyebrow">Manga</p><h1>{work.title}</h1>{work.original_title?<p className="work-original">{work.original_title}</p>:null}<p className="work-byline">{creators.join(" · ")||"Autore non disponibile"}{work.release_year?` · ${work.release_year}`:""} · {publicationLabel}</p>{work.genres?.length?<div className="meta-pills">{work.genres.slice(0,5).map((genre:string)=><span key={genre}>{genre}</span>)}</div>:null}{work.description?<p className="work-description">{work.description}</p>:null}</div></section><section className="personal-dashboard" id="personale"><div className="personal-dashboard-head"><div><span className="eyebrow">La mia scheda</span><h2>{work.title} nella mia libreria</h2></div><WishlistToggle profileId={profile.id} workId={work.id} returnPath={"/library/manga/" + work.id}/></div><div className="personal-metrics"><div><span>Stato</span><strong>{statusText}</strong><small>Stato personale</small></div><div><span>Progresso</span><strong>{currentVolume?`Vol. ${currentVolume}`:"—"}{currentChapter?` · Cap. ${currentChapter}`:""}</strong><small>Punto raggiunto</small></div><div><span>Posseduti</span><strong>{ownedCount}{totalVolumes?` / ${totalVolumes}`:""}</strong><small>{canonicalEdition?.name??"Edizione da scegliere"}</small></div><div><span>Il mio voto</span><strong>{libraryEntry?.rating??"—"}{libraryEntry?.rating?" / 10":""}</strong><small>Personale</small></div></div><div className="personal-editors"><form action={updateMangaState} className="personal-editor-card"><input type="hidden" name="workId" value={work.id}/><label>Stato<select name="status" defaultValue={state}><option value="PLANNED">Da iniziare</option><option value="IN_PROGRESS">In lettura</option><option value="COMPLETED">Completato</option><option value="PAUSED">In pausa</option><option value="DROPPED">Abbandonato</option></select></label><button type="submit">Salva stato</button></form><form action={updateMangaProgress} className="personal-editor-card progress-card-editor"><input type="hidden" name="workId" value={work.id}/><label>Volume<input name="currentVolume" type="number" step="1" min="0" max={totalVolumes??undefined} defaultValue={currentVolume??""} placeholder="25"/></label><label>Capitolo<input name="currentChapter" type="number" step="0.01" min="0" defaultValue={currentChapter??""} placeholder="201"/></label><button type="submit">Aggiorna progresso</button></form><form action={updateMangaPersonal} className="personal-editor-card personal-notes-editor"><input type="hidden" name="workId" value={work.id}/><label className="favorite-toggle"><input type="checkbox" name="favorite" defaultChecked={Boolean(libraryEntry?.favorite)}/> Preferito</label><label>Voto<input name="rating" type="number" min="0" max="10" step="0.5" defaultValue={libraryEntry?.rating??""} placeholder="9.5"/></label><label className="notes-field">Note<textarea name="notes" defaultValue={libraryEntry?.notes??""} placeholder="Annotazioni personali..."/></label><button type="submit">Salva scheda</button></form></div></section><DetailTabs/><section id="panoramica" className="detail-section overview-grid"><article className="overview-card"><span className="eyebrow">Opera</span><h2>Panoramica</h2><dl><div><dt>Autore</dt><dd>{creators.join(", ")||"—"}</dd></div><div><dt>Stato editoriale</dt><dd>{publicationLabel}</dd></div><div><dt>Volumi</dt><dd>{totalVolumes??"—"}</dd></div><div><dt>Capitoli</dt><dd>{work.total_chapters??"Da verificare"}</dd></div></dl></article><article className="overview-card note-preview"><StickyNote size={20}/><span className="eyebrow">Le mie note</span><p>{libraryEntry?.notes||"Nessuna nota personale. Puoi aggiungerla nella tua scheda senza modificare i dati globali dell'opera."}</p></article></section><section id="volumi" className="detail-section volumes-stage"><div className="section-heading detail-heading"><div><span className="eyebrow">Catalogo + collezione</span><h2>Tutti i volumi</h2></div><span>{totalVolumes?`${totalVolumes} pubblicati`:"Totale da verificare"}</span></div><p className="subtitle detail-explainer">L'intera serie resta visibile. Il progresso di lettura e il possesso sono indipendenti.</p><VolumeLegend/>{volumes.length&&canonicalEdition?.id?<div className="volume-grid volume-grid-visual">{volumes.map(volume=>{const owned=ownedIds.has(volume.id);const read=currentVolume!==null&&volume.unit_number<=currentVolume;const current=currentVolume===volume.unit_number;return <article key={volume.id} className={`volume visual-volume interactive-volume ${owned?"owned":""} ${read?"read":""} ${current?"current":""}`}><div className="volume-spine"><span>{String(volume.unit_number).padStart(2,"0")}</span></div><div className="volume-copy"><strong>Volume {volume.unit_number}</strong><span>{current?"Volume attuale":read?"Letto":"Da leggere"}</span><small>{owned?"Posseduto":"Non posseduto"}</small></div><form action={toggleOwnedVolume} className="volume-toggle-form"><input type="hidden" name="workId" value={work.id}/><input type="hidden" name="editionId" value={canonicalEdition.id}/><input type="hidden" name="unitId" value={volume.id}/><button className="volume-action" type="submit" aria-label={owned?`Rimuovi volume ${volume.unit_number} dai posseduti`:`Segna volume ${volume.unit_number} come posseduto`}>{owned?<Check size={14}/>:<Circle size={14}/>} {owned?"Posseduto":"Segna"}</button></form></article>})}</div>:<div className="catalog-notice"><LibraryBig size={22}/><div><strong>Volumi non ancora materializzati nel catalogo.</strong><p>Quando il provider restituisce il totale, l'import crea automaticamente le unità. Per serie con numerazione complessa useremo i dati reali dell'edizione.</p></div></div>}</section><section id="capitoli" className="detail-section two-up-detail"><article className="info-panel"><BookOpen size={22}/><div><span className="eyebrow">Capitoli</span><h2>{work.total_chapters?`${work.total_chapters} censiti`:"Numerazione da verificare"}</h2><p>Il progresso può essere salvato subito come “letto fino al capitolo X”. L'elenco dettagliato viene creato solo da una fonte affidabile, per non sbagliare speciali e numeri decimali.</p></div></article><article className="info-panel"><Layers3 size={22}/><div><span className="eyebrow">Relazione</span><h2>Opera ≠ edizione</h2><p>Leggi l'opera una volta sola; puoi però possedere volumi appartenenti a edizioni diverse senza duplicare {work.title} nella libreria.</p></div></article></section><section id="edizioni" className="detail-section"><div className="section-heading detail-heading"><div><span className="eyebrow">Edizioni</span><h2>Le versioni di {work.title}</h2></div></div>{editions?.length?<div className="edition-row">{editions.map(edition=><article key={edition.id} className={`edition-choice ${edition.id===canonicalEdition?.id?"selected":""}`}><strong>{edition.name}</strong><span>{[edition.publisher,edition.language,edition.publication_year].filter(Boolean).join(" · ")||"Dati da arricchire"}</span></article>)}</div>:<div className="catalog-notice"><div><strong>Nessuna edizione ancora collegata.</strong><p>Il catalogo dell'opera resta comunque valido e potrà essere arricchito in seguito con edizioni italiane, ISBN e copertine specifiche.</p></div></div>}</section></main>}
